{
  "name": "amemory",
  "version": "2.0.0",
  "private": true,
  "description": "Luxury personal blog — React + Python FastAPI + SQLAlchemy",
  "scripts": {
    "postinstall": "npm install --prefix client --include=dev",
    "build": "npm run build --prefix client",
    "render:build": "pip install -r backend/requirements.txt && npm install --include=dev && npm install --prefix client --include=dev && npm run build --prefix client",
    "start": "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port 3000",
    "predev": "npx --yes kill-port 3000",
    "dev": "concurrently -n api,client -c blue,magenta \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd backend && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 3000",
    "dev:client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
